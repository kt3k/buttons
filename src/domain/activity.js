class Activity {
  constructor ({ id, type, user, button, info, date }) {
    this.id = id
    this.type = type
    this.user = user
    this.button = button
    this.info = info
    this.date = date
  }
}

module.exports = Activity
